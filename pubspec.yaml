name: workspace
description: Forja monorepo workspace
publish_to: none

environment:
  sdk: ^3.11.0

# Run the app from apps/forja:
#   cd apps/forja && flutter run -d macos

dev_dependencies:
  melos: ^6.3.0
  flutter_lints: ^6.0.0
