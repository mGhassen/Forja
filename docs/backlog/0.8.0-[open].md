# 0.8.0 — Sidi Bou Said

**Status:** open  
**Major:** v0 Tell & land  
**Arc:** 0.8.x · app shell rework  
**Author:** Ghassen MENAOUAR

*Sidi Bou Said* — blue-and-white cliff village; aesthetic and interface arc before v1 Souk & métier.

## RFCs

| RFC | Slice | Status after ship |
|-----|-------|-------------------|
| [RFC-023](../../rfc/023-[partial]-app-shell-redesign.md) | Shell foundation + Home/Settings proof | `[partial]` — slice 1 in progress |
| [RFC-016](../../rfc/016-[draft]-lazy-tab-mounting.md) | Lazy tab cache (already in `main_screen.dart`) | `[draft]` — no change |

## Issues

| Issue | Slice | Status after ship |
|-------|-------|-------------------|
| — | — | — |

## Migration

| Phase | Slice | Status after ship |
|-------|-------|-------------------|
| — | Migration complete — no new phases | — |

## Shipped

- [x] RFC-023: `NavDestination` typed registry; settings `_navMeta` removed
- [x] RFC-023: `ShellScaffold` + `ShellNavRail` + `ShellBottomNav` + `ShellBody`
- [x] RFC-023: `MainScreen` slimmed to coordinator
- [x] RFC-023: Home + Settings body-only (no nested `Scaffold`)
- [x] Visual pass: `shell_tokens` wired into nav chrome
- [x] `flutter analyze` clean on touched shell files
- [ ] Smoke: desktop rail, logo inset, tab switch, single background on Home/Settings
- [ ] Smoke: mobile portrait bottom nav scroll, safe area, tab switch
- [ ] Smoke: Settings → Navigation Bar reorder/toggle persists
- [ ] Smoke: `ShellBus.requestTab('search')` still switches tabs

## Deferred → 0.8.x patches

- [ ] RFC-023 slice 2: remaining 17 tabs → body-only
- [ ] RFC-023 slice 2: IPTV immersive mode, Music sidebar rules

## On hold

[1.0.0 — Bab Souika](1.0.0-[draft].md) (player overlay, providers, casting) resumes after 0.8.x shell is stable.
