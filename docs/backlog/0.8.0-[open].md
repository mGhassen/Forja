# 0.8.0 — Sidi Bou Said

**Status:** open  
**Major:** v0 Tell & land  
**Arc:** 0.8.x · app shell rework  
**Author:** Ghassen MENAOUAR

*Sidi Bou Said* — blue-and-white cliff village; aesthetic and interface arc before v1 Souk & métier.

## RFCs

| RFC | Slice | Status after ship |
|-----|-------|-------------------|
| [RFC-023](../../rfc/023-[partial]-app-shell-redesign.md) | Shell + all nav tabs body-only + shell search bar | `[partial]` — slice 1 shipped; slice 2 polish |
| [RFC-016](../../rfc/016-[draft]-lazy-tab-mounting.md) | Lazy tab cache (already in `main_screen.dart`) | `[draft]` — no change |

## Issues

| Issue | Slice | Status after ship |
|-------|-------|-------------------|
| — | — | — |

## Migration

| Phase | Slice | Status after ship |
|-------|-------|-------------------|
| — | Migration complete — no new phases | — |

## Shipped

- [x] RFC-023: `NavDestination` typed registry; settings `_navMeta` removed
- [x] RFC-023: `ShellScaffold` + `ShellNavRail` + `ShellBottomNav` + `ShellBody`
- [x] RFC-023: `ShellSearchBar` in shell for Search tab
- [x] RFC-023: `MainScreen` slimmed to coordinator
- [x] RFC-023: default nav = Home · Search · Settings (+ hidden tabs re-enable in Settings)
- [x] RFC-023: all 20 nav tab roots body-only (no nested shell `Scaffold`)
- [x] Visual pass: `shell_tokens` wired into nav chrome
- [x] `flutter analyze` clean on touched shell + nav tab files
- [ ] Smoke: desktop rail, tab switch, single background
- [ ] Smoke: mobile portrait bottom nav, safe area, tab switch
- [ ] Smoke: Settings → Navigation Bar reorder/toggle persists
- [ ] Smoke: `ShellBus.requestTab('search')` + shell search bar

## Deferred → 0.8.x patches

- [ ] RFC-023 slice 2: IPTV immersive mode (hide global nav)
- [ ] RFC-023 slice 2: Music double-sidebar vs global rail rules
