# 1.2.x — Dabaghin

**Status:** draft  
**Since release:** 1.2.357 (`v1.2.357`)

User-facing changes for the next **1.2.x** patch. Update on every push; edit or remove bullets when later pushes correct earlier work. On ship, this file becomes `done/X.Y.Z-[released].md` (exact patch from the release tag) and a fresh `1.2.x-[draft].md` starts.

Use the [thematic groups](../../.cursor/rules/changelog.mdc#thematic-groups-forja) below — **add a `### Group` heading only when that group has bullets**; never leave empty group headers. Every bullet starts with `**Add:**` · `**Change:**` · `**Fix:**` · `**Remove:**`.

---

### Sources
- **Fix:** AniKoto / Miruro lists every playable server under a provider (VidPlay, Vidstream, …) instead of collapsing to one or two HLS rows.
- **Fix:** Server reliability Score nets stream −2 against server +2 on the same title (no phantom +2 when the stream failed first).
- **Fix:** Stremio Sources chips and results work after sync — lean cloud addon rows re-fetch their manifests (Torrentio no longer looks installed but empty).
