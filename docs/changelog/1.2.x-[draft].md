# 1.2.x — Dabaghin

**Status:** draft  
**Since release:** 1.2.365 (`v1.2.365`)

User-facing changes for the next **1.2.x** patch. Update on every push; edit or remove bullets when later pushes correct earlier work. On ship, this file becomes `done/X.Y.Z-[released].md` (exact patch from the release tag) and a fresh `1.2.x-[draft].md` starts.

Use the [thematic groups](../../.cursor/rules/changelog.mdc#thematic-groups-forja) below — **add a `### Group` heading only when that group has bullets**; never leave empty group headers. Every bullet starts with `**Add:**` · `**Change:**` · `**Fix:**` · `**Remove:**`.

---

### TV
- **Fix:** Series details keep seasons and episodes at the bottom of the screen so the hero backdrop stays open.
- **Fix:** Series details hero no longer overflows when the episode rail leaves little room — Play stays, title/meta shrink or hide.
- **Fix:** Details hero Play / Sources / My List move left and right on D-pad again (actions stay above the episode rail).
- **Fix:** D-pad up from Home catalog rows lands on hero Play (not stuck under the hero).
- **Fix:** Anime and Asian Drama hero Play / Details are in the D-pad graph; up from the first row focuses Play.
- **Fix:** Anime mood posters ↑ return to mood chips; Continue Watching ↑ focuses hero Play; empty Continue Watching leaves the D-pad graph.
- **Fix:** Anime details SUB / DUB are D-pad focusable next to Play (↑ still focuses Back).
- **Fix:** Continue Watching close/info chips on Home, Anime, and Asian Drama no longer steal D-pad focus on TV.
- **Fix:** My List grid D-pad moves between posters (↑/↓/←/→); remove chip is mouse-only on TV; entering the tab focuses the first poster.
- **Fix:** Trakt / MDBlist list detail opens in the shell overlay (Back returns to Settings → Lists).
- **Fix:** Settings categories open in the shell overlay; entering Settings focuses the first category; **OK** flips toggles / cycles dropdowns without nested focus steal; Features rows are D-pad focusable.
- **Fix:** IPTV player opens in the shell overlay; category pin no longer steals D-pad; source picker rows are focusable.
- **Fix:** Live Matches: enter from nav restores grid focus; timeline granularity and CDN Channels/Sports chips are in the D-pad graph; Jump to now is focusable.
- **Fix:** Live Matches timeline ↑/↓ moves between hour rows (←/→ within the hour); IPTV EPG channel rail is D-pad focusable; M3U playlists/player open in the shell overlay.
- **Fix:** IPTV channel search close/clear no longer steals D-pad from the result list; Settings provider-order tabs are D-pad focusable.
- **Fix:** Media details D-pad up from Play focuses the back chevron; OK on an episode selects it and moves to the play icon, OK on play starts streaming.
- **Fix:** Search (tab + overlay, Home/Anime/Asian Drama) focuses the field on open; OK edits, right goes to clear (X), left returns, down from X goes to result cards; left from cards returns to the aligned suggestion; left from the search field no longer jumps to the nav (use Down → suggestions → Left); leaving to the nav and coming back restores the search field (not Home’s search).
- **Fix:** Stream loading overlay focuses Select providers / Cancel and D-pad navigates the server list.
- **Fix:** When stream resolve fails, Try again autofocuses on TV; ↓ reaches Close.
- **Fix:** Live Matches loading dialogs are cancellable (Back / Cancel) instead of trapping the remote.
- **Fix:** Sources panel Kind tabs and fetch Cancel are D-pad focusable on TV.
- **Fix:** Empty IPTV / M3U / Live Matches CTAs autofocus on TV (Reload, Add portal / URL, Refresh).
- **Fix:** Live Matches stream / server / channel pickers autofocus the first row on TV.
- **Fix:** Settings confirm dialogs are D-pad focusable (Cancel / Confirm).
- **Fix:** Server reliability on TV uses ↑/↓ to reorder and a focusable Reset order.
- **Change:** Player progress bar (MediaKit, ExoPlayer, trailers): OK arms the thumb, left/right nudge, OK commits; up from the bar focuses Back.
- **Fix:** Remote volume up/down keys adjust player volume even while chrome is focused.

### UI
- **Fix:** Details top-left back chevron pops the page again (movies/TV, Anime, Asian Drama).
- **Fix:** Website download page loads installers from the release CDN again.
- **Change:** Download page shows the latest release title next to the platform picker (full notes on Changelog).
- **Fix:** Download FAQ Close collapses the answer again.
- **Change:** Home Categories menu on TV highlights the focused row in white.
- **Change:** Home, Anime, and Asian Drama search icons stay gray until hover/focus.
- **Fix:** Search result cards on TV deselect when you move left to recommendations.
- **Change:** Web login success stays in the login card (loading → signed-in), without opening a separate confirmation page.
- **Change:** After Web login, Forja comes back to the front when you close the browser tab (or when it auto-closes).
- **Change:** Account → Connections uses clearer session cards with location flag, IP, and separate signed-in / last-active rows.
- **Fix:** Losing the cloud session returns to sign-in and clears account IPTV portals — no more silent Guest mode with the previous account still loaded.
- **Fix:** Home shows Stremio catalog rails again (e.g. **Cinemeta · Top**) — catalog fetches no longer die when a torrent cancels, and lean synced addons re-fetch full manifests.

### Features
- **Change:** Android / Linux app id is `com.forjahq.app` (same as iOS/macOS) — uninstall the old `com.forja.app` build before updating.
- **Change:** Sign out and session loss reset account-bound local data (IPTV portals, synced prefs) before the desktop sign-in screen.
- **Fix:** macOS release builds keep you signed in after quit — Forja account session (and other Keychain secrets) no longer fail to save with a silent Keychain error.

### Sources
- **Fix:** Anime Vidwish is only tried when Anikoto found a catalog embed — dead `/stream/ani/…` URLs no longer waste the server race.

### Player
- **Fix:** Episodes panel Search on Android TV no longer doubles the “Search” placeholder; Auto next focus tints the skip icon green — no gray hover/focus pill on the switch.
- **Fix:** Choose player (external handoff) on Android TV moves ↑/↓ in list order — ↑ from the first row goes to Close, not the last app.
- **Fix:** Android TV MediaKit shows video again (was audio-only / black picture) — Impeller off on TV + MediaCodec embed output instead of OpenGL.
- **Add:** ExoPlayer floating menus for episodes, audio, subtitles, quality, and lean settings (speed + fit).
- **Fix:** Opening a player menu on TV moves D-pad focus into the menu (not the chrome underneath).
- **Fix:** Opening a player menu on TV focuses the currently selected option (speed, quality, audio, subtitles), not the first row.
- **Fix:** Closing a player menu on TV returns D-pad focus to the button that opened it.
- **Fix:** Player Settings menu rows show D-pad focus and move ↑/↓ like audio / quality.
- **Fix:** D-pad ↑/↓/←/→ walks options inside every player menu (audio, subs, quality, settings, episodes, source).
- **Fix:** Episodes panel on TV opens on the playing episode; ↑/↓ moves between episodes; ↑ from the first row reaches Season / Search; Search stays browse-only until OK; ↓ from the top bar returns to the list.
- **Fix:** From the progress bar on TV, ↑ focuses Next Episode or Skip Intro when those chips are showing (otherwise the back button).
- **Fix:** Player picker on TV moves ↑/↓ one option at a time (no jump from the selected row to the last).
- **Change:** Subtitle settings sliders on TV need OK before ←/→ adjust (OK again commits; Back cancels).
- **Fix:** Android TV player menus (MediaKit, ExoPlayer, trailer) focus one row at a time — gray highlight, no scale; ↑/↓ no longer need multiple presses per row.
- **Change:** Android TV trailer controls match desktop — play / ±10s / mute on the left, audio / subtitles / quality / speed on the right.
- **Remove:** PiP and Cast buttons on Android TV player chrome (MediaKit and ExoPlayer).
- **Fix:** Android TV MediaKit shows the Player button again (switch built-in engine / external app).
- **Change:** Back button on movie and IPTV players sits a bit farther from the screen edge.
- **Change:** Torrent loading shows live peers, speed, and buffered size while the local engine starts.
- **Fix:** Starting a torrent no longer stutters the UI — engine job waits run off the main thread.
- **Fix:** Android ExoPlayer plays HLS and DASH streams again (missing Media3 modules).
- **Fix:** Android ExoPlayer no longer drops the stream when the video surface remounts (KissKh / HLS on TV).
- **Fix:** Subtitles menu lists embedded in-stream tracks again (MediaKit) — not only Load from file / online languages.
- **Fix:** Subtitle Settings no longer crashes on Android TV when size is the TV default (52).

### Live & IPTV
- **Change:** Deal portals is account-gated (admin feature flag) — hidden until enabled; still needs catalog credits to spend.
- **Fix:** Dealt portals show the same accent + **NEW** badge as scrape until you hover or focus them.
- **Fix:** Account feature flags and IPTV credits refresh when you open IPTV or bring the app back — no restart needed after admin toggles.
- **Change:** After search or returning from the player, the selected IPTV category scrolls into view as the 4th row instead of jumping to the top.
