# 1.5.x — Berka

**Status:** draft  
**Since release:** 1.5.21 (`v1.5.21`)

User-facing changes for the next **1.5.x** patch. Update on every push; edit or remove bullets when later pushes correct earlier work. On ship, this file becomes `done/X.Y.Z-[released].md` (exact patch from the release tag) and a fresh `1.5.x-[draft].md` starts.

Use the [thematic groups](../../.cursor/rules/changelog.mdc#thematic-groups-forja) below — **add a `### Group` heading only when that group has bullets**; never leave empty group headers. Every bullet starts with `**Add:**` · `**Change:**` · `**Fix:**` · `**Remove:**`.

---

### UI
- **Fix:** Official pack install list scrolls in place so **Install** stays visible (same layout as Update Forja).
- **Fix:** Nav rail tab labels (focus / hover) sit below the underline with room to breathe — no clipped bottoms.

### TV
- **Fix:** Android TV guest (Settings-only rail) D-pad lands on get-started / Settings content; **→** enters the hub after cards close; Back no longer parks on the lone Settings icon.
- **Fix:** Installing official hub packs (guest onboarding or Settings) turns their Features / navbar tabs on immediately — no pack off/on needed.
- **Fix:** After sign-in and profile pick, the shell opens on your starred tab instead of staying on Settings once packs finish installing.
