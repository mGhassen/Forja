# 1.2.365 — Dabaghin

**Status:** released  
**Version:** 1.2.365 (`v1.2.365` — Dabaghin)

---

### Player
- **Fix:** Torrent seek bar picks up duration after open so click-to-seek works (was stuck at 0:00 / 0:00).
- **Fix:** Cancelling torrent start no longer flashes a red error screen (loading overlay was dismissed twice).
- **Change:** Torrent loading shows live peers, speed, and buffered size while the local engine starts.

### Sources
- **Fix:** Replay / Continue Watching no longer opens stale stream links — expired CDN tokens and dead HLS segments drop the cache and re-check servers.
- **Fix:** AniKoto / Miruro lists every playable server under a provider (VidPlay, Vidstream, …) instead of collapsing to one or two HLS rows.
- **Fix:** Server reliability Score nets stream −2 against server +2 on the same title (no phantom +2 when the stream failed first).
- **Fix:** Server reliability scores server and stream together — extract OK + dead streams nets +2/−2 (not a lone +2); Cancel / unfinished checks add nothing.
- **Fix:** Stremio Sources chips and results work after sync — lean cloud addon rows re-fetch their manifests (Torrentio no longer looks installed but empty).
- **Add:** Sources → Nuvio has an **All** chip that selects every scraper at once.
