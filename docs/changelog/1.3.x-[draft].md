# 1.3.x — Elblat

**Status:** draft  
**Since release:** 1.3.35 (`v1.3.35`)

User-facing changes for the next **1.3.x** patch. Update on every push; edit or remove bullets when later pushes correct earlier work. On ship, this file becomes `done/X.Y.Z-[released].md` (exact patch from the release tag) and a fresh `1.3.x-[draft].md` starts.

Use the [thematic groups](../../.cursor/rules/changelog.mdc#thematic-groups-forja) below — **add a `### Group` heading only when that group has bullets**; never leave empty group headers. Every bullet starts with `**Add:**` · `**Change:**` · `**Fix:**` · `**Remove:**`.

---

### Live & IPTV
- **Fix:** Live Matches embeds on Android TV play again — loads under the catalog site (same as desktop) instead of a bare embed URL that tripped the host lock (“Remove sandbox attributes…”).

### UI
- **Change:** Switching profiles no longer waits for home-feed and engine warm to finish — the avatar splash dismisses like the intro splash, and IPTV portals only sync when that profile has IPTV.
- **Fix:** Account settings on the web (Playback, Features, Stremio, Nuvio) keep your edits instead of snapping toggles and fields back before you can save.
- **Fix:** Asian Drama hub and explore cards use wide 16:9 frames so KissKH landscape covers are no longer zoomed and cropped in portrait posters.

