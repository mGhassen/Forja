# 1.4.x — Atarin

**Status:** draft  
**Since release:** 1.4.232 (`v1.4.232`)

User-facing changes for the next **1.4.x** patch. Update on every push; edit or remove bullets when later pushes correct earlier work. On ship, this file becomes `done/X.Y.Z-[released].md` (exact patch from the release tag) and a fresh `1.4.x-[draft].md` starts.

Use the [thematic groups](../../.cursor/rules/changelog.mdc#thematic-groups-forja) below — **add a `### Group` heading only when that group has bullets**; never leave empty group headers. Every bullet starts with `**Add:**` · `**Change:**` · `**Fix:**` · `**Remove:**`.

### Features
- **Fix:** Asian Drama Plan to Watch no longer shows “Simkl failed” when KissKH has no TMDB match — the local pin still saves.
- **Fix:** Stremio search links from Sources and the Home Search overlay work again — `stremio:///search` opens Search with your query; genre searches (e.g. horror) query addons too.

### UI
- **Add:** Hub details show premiere and episode air dates — upcoming dramas/movies get a **Coming soon** hero with the formatted date; episode rows show orange future air dates and block Play until aired (packs emit `premiereDate`, `airDate`, `aired` on catalog meta).
- **Change:** My List is a catalog hub pack — install it under Settings → Sources → Forja → Hubs; the tab no longer appears when the pack is off.
- **Change:** My List layout is pack-composed (`kit.stack` + `kit.menu` + `kit.tabs` + `kit.list` grid) — poster grid, not per-kind rails.
- **Fix:** My List kind and status menus use the same shell top bar slot as other hubs (`PluginHubCatalogTopBar`); pack `kit.menu` / `kit.tabs` are hoisted there — not a separate in-page menu.
- **Fix:** My List **Asian Drama** kind filter includes hub bookmarks saved with `mediaType: drama` / `open.surface: drama`, not only legacy `asian_drama` rows.
- **Fix:** Continue Watching rows refresh after playback or Simkl import without scrolling away first — hub history listeners always reload in-progress entries.
- **Fix:** Local Continue Watching shows titles again after ~10s of playback — the row no longer waits for 2% of a long runtime before listing saved progress.

### Live & IPTV
- **Fix:** IPTV VOD series/movie details no longer fail after the plugin installs — `iptv-vod` returns a protocol envelope array the host can parse (not a bare object that looked like “no answer”).
- **Fix:** IPTV VOD hub details work right after installing the IPTV plugin — missing JS is hydrated on first open instead of failing until app restart.
- **Change:** IPTV Movies and Series details use the shared hub details kit with a portal-only catalog plugin; optional TMDB enrich comes from the IPTV VOD pack — not a built-in TMDB client.
- **Change:** Live sports plugins merged into one pack per site — Settings → Sources → Forja → **Live** and Settings → Forja Sports use **Catalog** / **Provider** tabs with independent toggles per site (Streamed, PPV, StreamFree, …).
- **Fix:** Forja Sports live plugin on/off is app-owned again — Settings capability prefs gate catalog/resolve; external live packs no longer ship `enabled` / `catalogEnabled` / `resolveEnabled`.
- **Add:** Settings → Playback (Android TV, admin) — **IPTV live buffer**: Auto by resolution or force 15 / 20 / 30 seconds of MediaKit demuxer cushion (with matching RAM).

### Sources
- **Change:** Builtin torrent indexers (Knaben, Pirate Bay, Nyaa, Torrentio, …) ship in the **ForjaHQ Torrent** plugin pack — enable or disable each indexer under Settings → Sources → Forja → Torrent. Sort order, disk cache, and connection limits stay under **Torrent search** / **Torrent engine** on the same page.
- **Remove:** Duplicate **Torrent providers** toggles under Settings → Sources — indexer on/off is only in the ForjaHQ Torrent pack expansion (same as stream providers).
- **Fix:** Sources **Torrents** tab searches on open again — default provider chip is **All** (was `none`, which skipped every indexer).
- **Fix:** Torrent Sources lists rows as each indexer returns again — progressive paint no longer waits on title filter before showing the first hits (filter still runs when the search finishes).
- **Fix:** Torrent search starts after installing the ForjaHQ Torrent pack without closing Sources — the panel refreshes indexers when packs change and no longer stays on a stale **None** chip.
- **Fix:** Settings provider toggles (Stremio, Nuvio, Forja plugins) no longer jump to the bottom of the list when you enable or disable one.
- **Fix:** Torrent search no longer crashes the app on desktop — indexers run on one JS runtime sequentially instead of spawning nine parallel VMs.
- **Fix:** Torrent indexers (Torrentio, YTS, Nyaa, …) no longer return empty when Engine or Nuvio sources load in parallel — torrent search VMs are no longer killed by provider cancel.
- **Fix:** YTS torrent search uses live API hosts again (`yts.mx` DNS is dead).
- **Fix:** Torrent pack dev checkout (`FORJA_REPO_ROOT`) always loads fresh `plugins/torrent/*.js` instead of stale installed cache; pack reinstalls when manifest version bumps.
- **Fix:** Home TV series (e.g. Lanterns) no longer show “No Forja plugins are selected” when the providers pack is on — TMDB episode ids like `12345:S1E1` are not mistaken for a single provider chip.
- **Change:** Settings → Forja plugins shows the manifest URL while a pack installs; lean stubs appear as a pending row until scripts land. IPTV and hub details show a clear “still downloading” message instead of a silent failure when scripts are not ready yet.
- **Change:** IPTV VOD details pack installs from `plugins/iptv/vod/` — Settings → Forja plugins lists it under **IPTV**, not **Hubs** (legacy `plugins/hubs/iptv/` manifest URLs still work).
- **Fix:** Desktop engine init no longer fails on a stale `libffi` missing the media-extra FFI export — splash hub prefetch runs through EngineJS instead of logging `RustLib.init()` errors.
- **Fix:** KissKh (Asian Drama) Sub API tracks decrypt in the provider pack and ship plaintext to the player — timing matches kisskh.co instead of mistimed HLS mux tracks.
- **Fix:** Startup plugin update also refreshes local checkout packs (file paths) — Settings no longer shows stale versions until you tap Reload.
- **Change:** Engine and Nuvio plugin scripts download to on-device storage on startup (and when updated) — splash shows install steps and a thin progress bar in the existing bottom status area; if download stalls or fails, **Continue in background** opens the app while install keeps going; Settings Reload still force-refreshes a pack; Remove deletes its cached files.
- **Remove:** WebStreamr provider and Settings hub — stream resolve uses JS engine providers only.
- **Remove:** Trakt login, sync, scrobble, and Lists tab — Simkl remains the connected tracker.
- **Remove:** Built-in embed resolver (VidLink, VSEmbed, WebView sniff) — Home/details play uses engine JS providers only.
- **Remove:** Rust resolver-engine crate — server reliability scoring lives in engine.
- **Remove:** KissKh Rust crate — Episode `kkey` crypto lives in engine with other provider host hooks; browse/extract stay in hub/provider JS.
- **Remove:** Settings → Sources **Server reliability** — legacy extractor order / Score UI (engine plugins only now).

### Player
- **Fix:** Hub players (Anime, Asian Drama) Episodes panel shows per-episode stills and TMDB titles from pack enrich again — not the same show backdrop on every row.
- **Change:** Bottom-bar **Sources** / **Source** buttons show the provider and active server stacked (e.g. Videasy over Yoru) instead of the provider name alone.
- **Fix:** Subtitle language list resolves ISO / OpenSubtitles codes to real names (endonyms when known, else English) instead of raw `Wo` / `Chr` / `Ckb` codes.
- **Fix:** Desktop Subtitle Settings delay − / + responds to mouse clicks again.

### UI
- **Fix:** Home **Featured This Month** shows again when the month window is thin — the pack backfills from popular and the feed row falls back below the hero if the bleed slice is empty.
- **Fix:** Home hub catalog cards show media type again under the title (`2026 • FILM` / `TV`) — not year alone.
- **Fix:** Hub browse hero carousel shows media type again (`FILM` / `TV`) — same as the details hero meta row.
- **Fix:** Home and Asian Drama browse heroes show genre chips in the meta row (like Anime) — Home from TMDB list data; Drama fills in from TMDB when a match id exists.
- **Remove:** Legacy movie/TV details screen — Home, My List, Stremio catalog, and search addon rows open the shared hub details kit.
- **Change:** Home movie and TV details use the same hub details screen as Anime and Asian Drama — season/episode picker, sources panel, and pack-backed recommendations/collections.
- **Add:** Settings → Playback shows **Preferred subtitle language** again (default English) — controls auto-pick for in-stream and online subs.
- **Change:** Settings → Profile & account on desktop (signed out) uses the same **Sign in** → code + QR flow as cold start — one loading spinner while the code is prepared; click the QR on desktop to open `/connect` in your browser.
- **Change:** Hub Continue Watching (Anime, Asian Drama, Arabic) reads pack-scoped catalog watch history; Home movie/TV rows still come from your existing watch history store.
- **Fix:** Home Continue Watching shows in-progress movies and series again after you pause playback from Home.
- **Fix:** Simkl background sync runs on launch and resume again — in-progress watching seeds Home and Asian Drama Continue Watching (throttled ~15 min); Sync Now is unchanged.
- **Change:** Product analytics (PostHog) no longer records session replay in the app or on the web portal — allowlisted usage events and pageviews only.
- **Add:** Home, Anime, Asian Drama, and Arabic now come from the new **ForjaHQ Hubs** plugin pack — rows, moods, and ranked lists are defined by the pack instead of being baked into the app.
- **Change:** Hub tabs use the shared cinematic hero, Continue Watching, and pack-backed Search (Anime / Asian Drama / Arabic top bar).
- **Change:** Home / Anime / Asian Drama / Arabic nav entries come from the Hubs pack (`nav` specs) — labels, accents, and tab bodies follow the pack.
- **Remove:** Legacy Home / Anime / Asian Drama / Arabic browse screens — hub tabs always use Catalog Shell from the Hubs pack.
- **Change:** Hub Films / Series / Categories menus refetch Catalog Shell rails from the Hubs pack (not a dead chrome toggle).
- **Change:** Home, Anime, and Asian Drama each use their own hub pack manifest (one URL per tab).
- **Change:** Engine packs are external — install manifest URLs in Settings or sync them from your profile; the app no longer bakes pack inventory into the binary.
- **Change:** Cold start hydrates installed packs on the splash, then prefetches the default hub from pack `layout` (`feed` when declared, otherwise rails above Continue Watching) into the shared catalog cache — BootCache parity without a baked-in rail list.
- **Add:** Arabic hub fills rails from Larozaa categories and Brstej; Search merges Larozaa, DimaToon, and Brstej.
- **Change:** Hub catalog rows load only when they scroll into view; scrolling a row toward the end fetches the next upstream page, and the row below starts warming as soon as the one above is visible.
- **Change:** Asian Drama **Popular** is a ranked KissKH rail from the hub pack (no Dart TMDB `host.popular_asian` widget).
- **Add:** Larozaa, DimaToon, and Brstej are Forja **provider** plugins (`types: arabic`) for stream extract; the Arabic hub pack only browses and loads episode lists.
- **Fix:** Larozaa and Brstej Arabic providers unpack third-party server pages in JS and return direct HLS/MP4 streams — no embed hop or WebView resolver at play time.
- **Fix:** Arabic hub accepts the live Larozaa redirect host (`laaroza.pics`) so rails stop loading empty.
- **Change:** Arabic hub layout matches Home (cinematic spotlight + bleed row + Continue slot + ranked Brstej + category rails).
- **Add:** Web Profile settings → **Forja plugins** (pack manifest URLs sync with the app, menu before Stremio addons).
- **Change:** Web Profile settings save immediately on edit — no Save changes button.
- **Fix:** Home top-bar **Search** and **Cmd+F** open the same Search overlay (structured TMDB + addons) — pack declares `host_search`; Anime / Asian Drama keep pack-only hub search.

### Sources
- **Add:** Settings → Sources → Stremio addon rows have a switch next to trash to enable or disable an installed addon without removing it.
- **Fix:** VidCore resolves streams again — page scrape no longer sends the header that made vidcore.net return 403.

### UI
- **Fix:** Anime and Asian Drama enrich companions fill missing episode stills on details (`meta.videos`) when the pack has no per-episode thumbnail — host only renders enriched meta.
- **Fix:** Hub **View details** opens immediately from the card/hero meta — no wait on AniList/KissKH details or TMDB enrich; a second tap cannot stack another details page.
- **Fix:** Anime hub loads in one batched AniList feed (not ten parallel GraphQL calls) — spotlight, hero, and rails paint in seconds instead of minutes.
- **Fix:** Anime / Asian Drama / Arabic details open in the shared hub kit screen — pack `details` + TMDB enrich sections, not feature-specific Dart screens.
- **Fix:** Anime and Asian Drama details hero show the My List pin (+) again — same glass control as Home and movie details.
- **Fix:** Anime and Asian Drama details show the trash control next to Resume again — clears watch progress and that title’s cached streams.
- **Fix:** Anime hub details show pack-declared **SUB** / **DUB** play filters again — grouped kit buttons from the hub `filters` action; play and Sources respect the selection.
- **Change:** Android TV hub and details heroes use a smaller TMDB title logo so more meta fits under genres.
- **Fix:** Anime spotlight hero enriches with TMDB like details (English search title, season/part suffix strip, details cache reuse).
- **Fix:** Anime and Asian Drama hub rails no longer flash-reload when you return from details — pack filter metadata loading no longer clears painted rows.
- **Fix:** Asian Drama spotlight hero enriches with TMDB again (title normalize, film vs series hint, enrich on cache hit, reuse details cache).
- **Fix:** Asian Drama / Anime hero TMDB enrich prefers KissKH/AniList embedded TMDB ids and uses the same scored title match as details — ambiguous titles (e.g. Mousetrap) no longer stay on bare KissKH thumbs in the carousel.
- **Fix:** Asian Drama details TMDB enrich is cached by KissKH id — reopen the same title no longer rematches or re-plays the enrich animation.
- **Fix:** Hub tabs (Home / Anime / Asian Drama / Arabic) keep rails and hero painted on rebuild and tab return — no full shimmer reload when Catalog Cache still has the feed.
- **Fix:** Anime / Asian Drama hub TMDB enrich falls back to scored `search/multi` when the native matcher misses — spotlight hero and details get TMDB backdrops and `ids.tmdb` again.
- **Change:** Asian Drama / Anime TMDB spotlight enrich is a separate pack plugin (`enrich-tmdb` / `anime-enrich-tmdb`) — KissKH and AniList stay data-only; the host pipes rails/details through the enrich companion.
- **Fix:** Asian Drama / Anime details keep TMDB enrich (logo, synopsis, episode stills) in a process cache — leaving and reopening no longer re-searches TMDB every time.
- **Fix:** Asian Drama / Anime hero **View details** works again after TMDB spotlight enrich (was a silent no-op).
- **Fix:** Anime details keep AniList details / seasons spine / TMDB enrich in process cache — reopen no longer re-hammers GraphQL into HTTP 429.
- **Change:** Hub plugin off (Sources → Forja) removes that hub from Features and the nav rail; Features show/hide only applies among enabled hubs.
- **Fix:** Home watch-provider filter refetches TMDB discover with provider family + TV networks merge; Featured pads when thin; cross-row dedup runs in the Home pack `feed` action (not Dart).
- **Fix:** Stremio / ForjaHQ addon rows no longer blink when a hub pack plugin id collides with a Providers extract (Asian Drama hub is `kisskh-hub`).
- **Add:** Settings → Sources → Forja pack tabs include **Hubs** for Home / Anime / Asian Drama / Arabic catalog plugins (separate from Live **Catalog**).
- **Add:** Settings → Sources → Nuvio and Forja pack rows have a switch to enable or disable that pack (Forja keeps per-plugin toggles when you turn the pack back on).
- **Fix:** Forja pack on/off in Settings sticks — Official / FORCE install no longer turns packs back on after you disable them.
- **Change:** Settings → Forja Sports **Catalog** uses the same expandable pack rows as **Live plugins** (name, path, version, expand for toggles).
- **Change:** Settings → Sources → Forja pack rows show the manifest URL under the pack name.
- **Change:** Settings → Sources → Forja lists installed packs under **Providers** / **Live** / **Catalog** / **Hubs** (not one flat dump); each row shows its kind (Hubs include Home / Anime / Asian Drama / Arabic).
- **Change:** Settings → Sources → Jackett and Prowlarr show only for admin accounts.
- **Change:** Android TV Settings shows **Sources** (torrents, Stremio, Nuvio, Forja) when those play sources are on — WebStreamr, Debrid, Lists, and Data & backup stay hidden.
- **Change:** Settings → Sources → Forja lists every installed pack (Providers, Live, Catalog, community) with Live / Catalog tabs — not Providers-only — and sits first on the Sources page.
- **Change:** Settings → Forja Sports is empty until Live or Catalog plugins are installed (setup stays under Sources → Forja).
- **Fix:** Cloud / web account settings sync no longer flash-reloads the Settings page or steal focus when nothing local changed.
- **Fix:** Returning from background no longer resets Settings to the first category (selection survives resume sync / remount).
- **Fix:** Desktop Settings no longer jumps to Profile & account when the app returns from background (focus was landing on the first category tile and selecting it).
- **Fix:** Desktop Settings → Sources shows enable switches on Forja / Nuvio pack rows and Stremio addon rows again — leanback-only layout no longer hides them on desktop hybrid.
- **Fix:** Anime / Asian Drama hub cache skips companion TMDB enrich when meta already has the kit `_hubTmdbEnriched` marker (spotlight also skips on TMDB w1280 backdrop) — reopening details or revisiting spotlight no longer re-runs enrich on every cache hit.
- **Fix:** Asian Drama / Anime details always pipe through TMDB enrich once — KissKH/AniList already ship TMDB backdrop URLs, so the host no longer treats that as “already enriched” and skips episode stills / kit apply.
- **Fix:** Asian Drama / Anime hub details load TMDB cast, trailers, recommendations, and rotating hero backdrops again when `ids.tmdb` is set; browse hero uses `ids.tmdb` (not KissKH open id) for TMDB logo / backdrop gallery.
- **Fix:** Home hub genre discovery rows load again — only spotlight / featured / popular / new releases use the batched feed; genre and other rails fetch on their own.
- **Fix:** Home **Featured This Month** and **New Releases** rotate again — hourly random TMDB pages (Popular stays ranked; New Releases stays sorted by release date).
- **Fix:** Home **New Releases** no longer mixes decades-old titles — the row is limited to roughly the last 90 days (plus upcoming), including when a streaming-service filter is active.
- **Fix:** Home **Because you watched** row title spacing matches other rails again (no double gap under the header).
- **Fix:** Home cinematic hero shows again when the batched feed returns an empty spotlight slice — host falls back to a direct trending fetch instead of painting only the Featured bleed row.
- **Fix:** Updating a Hubs pack in Settings refreshes Home / Anime / Asian Drama when you go back — keep-alive tabs no longer keep the old layout for 15 minutes.

### TV
- **Fix:** Hub catalog kit D-pad — default browse registers focus graph; detail recommendation rails remember card index; hub search filter tokens and genre/country chips are focusable; Because-you-watched shuffle links to the rail below; Continue Watching Menu opens remove/info on TV.
- **Fix:** Android TV Settings → Sources D-pad walks the page again — pack expansion rows no longer trap ↓ in trailing switches/icons; Install and Stremio addon rows follow the same vertical focus path as other settings lists.
- **Fix:** Hub catalog tabs (Home / Anime / Asian Drama / Arabic) assign D-pad row order from the pack layout — ↓/↑ between rails, mood chips, Continue, and hero bleed rows no longer trap or skip.
- **Fix:** Hero bleed rows (Home Featured This Month, Anime Trending under spotlight) are focusable on D-pad — ↓ from View details lands on the bleed row instead of skipping it.
- **Fix:** Live Matches keeps D-pad focus on sport chips and match cards while Forja Live catalogs finish loading in the background.
- **Fix:** Android TV Settings category rail — D-pad Down/Up moves between Playback, Sources, and other categories again (focus no longer disposed mid-move when the selected tile rebinds).
- **Fix:** Android TV Settings → LAN pairing (Discover, Use, Pair, local-torrent toggle) and Sources → Forja pack refresh are D-pad focusable again.

### Player
- **Fix:** Videasy / Peakstorm seeks trim-reopen the playlist directly (no mpv `start` or mid-stream seek), fixing black screen on deep scrub and the brief wrong-frame jump before remount.
- **Fix:** Seek-bar scrubs on Peakstorm stay on the clicked time during remount instead of flashing to 0:00 while mpv resets.
- **Fix:** Peakstorm seek-bar scrubs no longer show **Reconnecting…** — that toast stays for network/stall recovery only.
- **Fix:** Switching Videasy (and other) quality rows in Sources while playing — e.g. Yoru 1080p → 720p — reopens the stream instead of ignoring the tap.
- **Fix:** New streams no longer start on the previous file's audio track — each open resets mpv `aid` and snaps to your preferred language once per source (not again when HLS refreshes renditions mid-playback).
- **Fix:** Deep scrub no longer stalls when HLS refreshes audio/subtitle tracks during seek — auto track pick runs once per open, not on every rendition update.
- **Fix:** Switching stream providers in the player keeps your playhead — cached extracts and engine re-resolve work again instead of reopening at 0:00.
- **Fix:** Audio track menu no longer guesses language names from unreliable HLS `lang` tags (`la` / `sv` / `no`) — shows **Audio 1**, **Audio 2**, … unless the mux title names the track; long menu scrolls instead of overflowing.
- **Fix:** Movie / series player chrome auto-hides again after idle when menus close, source checks finish, or status toasts clear — not only after D-pad lands on Play.
- **Fix:** Sources panel paints Forja / Torrents / Stremio / Nuvio tabs on the first frame instead of a blank Torrents-only flash while prefs hydrate.
- **Fix:** On Android TV, Sources category tabs (Forja / Torrents / Stremio / Nuvio) no longer reload when you hold OK — **→** focuses the reload icon, then **OK** refreshes.
- **Fix:** On Android TV, Sources **Filters** D-pad walks chips in reading order across wrapped rows (→ past the end of a line goes to the next line) with green focus chrome.
- **Fix:** On Android TV, player menus keep D-pad focus when the player refreshes underneath (tracks, stream checks, chrome rebuilds).
- **Fix:** Auto subtitles try in-stream mux tracks first again — scraped Levrx/Wyzie only load when nothing muxed matches your language. Peakstorm remount no longer re-applies a Levrx file over an already-selected in-stream track.
- **Fix:** Peakstorm deep scrub / post-seek remount re-applies your subtitle track instead of dropping external SRT after reconnect.
- **Fix:** Subtitle menu no longer lists sideloaded/cache tracks as In-stream after you pick an online sub — Wyzie/Levrx entries stay under the online list with the correct selection on reopen.
- **Fix:** On Android TV, opening Subtitles focuses the active track (or Off when subs are off) instead of always landing on Off.
- **Add:** Seek bar shows colored zones for intro/recap (blue), credits (seafoam), and next-episode preview or near-end (amber) when segment data is available.
- **Fix:** Details white Play opens Sources immediately again — Torrents / Stremio / Nuvio stay usable while Forja plugins install; only the Forja tab shows loading.
- **Fix:** Sources → Forja no longer spins on an empty list when no plugins are installed or selected — shows **No Forja plugins installed** or **Select at least one provider** instead.
- **Fix:** DahmerMovies resolves via `st.111477.xyz` stream links (direct play) instead of scraping `a.111477` file URLs that hit Cloudflare.
- **Remove:** Sources → Forja no longer shows a duplicate **111477** chip next to DahmerMovies (same script).
- **Change:** Movie, Anime, and Asian Drama green Play always races **Sources → Forja** provider plugins (no dedicated anime/drama player or webstreaming sniff path).
- **Remove:** Built-in webstreaming extractors (Videasy, WebStreamr race, Dart embed sniff) for VOD — stream resolve is provider JS + the Sources panel only.
- **Change:** Settings → Playback drops Webstreaming and Forja Auto toggles — green Play uses Forja providers when Forja is enabled.
- **Change:** Settings provider scoring shows Anime and Asian Drama only (legacy Movies/Series webstreaming order removed).
- **Fix:** Live Matches opens without waiting on dead Stremio VOD addon manifests — Forja Live catalogs start immediately; Stremio sport addons hydrate only when needed.
- **Fix:** IPTV EPG times follow this device’s regional timezone (Stalker/MAG no longer pinned to London; unix programme times convert to local).
- **Add:** On Android TV, hold OK ~1s on a channel in Favorites or Already watched to jump to that channel’s portal category while keeping focus on it (short OK still plays).
- **Fix:** On Android TV, ↑/↓ through IPTV categories lights only the focused row (opened group no longer keeps a second green mark while skimming).
- **Fix:** On Android TV, **↓** past the last portal in the Portals panel (including a filtered search) moves focus to channels instead of trapping in the list.
- **Fix:** Live Matches merges the same fixture when one catalog says `Away at Home` and another says `Home vs Away` (PPV ↔ Streamed reverse titles).
- **Add:** Live Matches cards and stream pickers show viewer counts from catalogs that report them (PPV, StreamFree, TimStreams); cards use the same red-dot badge as PPV, merged cards sum catalogs, and the stream picker header sums every listed stream.
- **Change:** Live Matches plugin labels, catalog Referers, and native unlock paths come from plugin pack config — not a hardcoded list of site plugin ids.
- **Fix:** WatchFooty stream picker lists every unlockable source (delta/echo/sigma/…), not only the first two.
- **Fix:** TimStreams Engine resolve unlocks embed pages to HLS instead of toasting “no playable stream” on every tap.
- **Fix:** StreamFree Live Matches cards show match backdrops again (thumbnails were relative and resolved against the wrong host).
- **Fix:** IPTV live recovery upgrades to **Auto** for installs still on Stable / old stall default (Classic unchanged) — stall is managed per source without picking a mode each time.
- **Fix:** Auto on Stalker no longer reconnects like stall was on — healthy demuxer cache holds when stall reopen is off (playhead often stays at 0).
- **Fix:** Reopening IPTV from the nav restores the last category (including Favorites / Already watched) and scrolls/focuses the last played channel without autoplay.

### Player
- **Fix:** Asian Drama / Anime hub play uses enrich TMDB id for online subtitles (Wyzie) and generic providers — no longer sends KissKh / AniList open id as TMDB.
- **Fix:** Sources panel remembers Forja/Nuvio provider chips under **All** (view filters) when you close and reopen the panel or open another title.
- **Fix:** Details Sources **Resume** highlights only the stream you actually watched — Videasy quality rows (1080p / 720p / 480p) no longer all show progress at once.
- **Remove:** Progress bar and RESUME label on Details Sources stream cards — resume still applies when you tap the row you watched.

---
