# RFC-003: PlayerController + overlay

## Components

- `PlayerScreen` + mobile/desktop players — `apps/forja/lib/shared/player/`
- `PlayerOverlayPanel` + `ServerGrid` — `apps/forja/lib/shared/design/`
- Shell wires nav; features open player via `AppRouter.openPlayer()`

## Controls

Previous/Next, AutoNext, Details, Shuffle, PiP, Watch Party (placeholder), server grid.

## Status

Design overlay stubs exist under `shared/design/` — not wired into player yet (Phase 3).
