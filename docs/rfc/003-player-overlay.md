# RFC-003: PlayerController + overlay

## Components

- `PlayerController` (`forja_player`) — media_kit wrapper
- `PlayerOverlayPanel` + `ServerGrid` (`forja_design`)
- `ForjaPlayerScreen` (`apps/forja`) — wires switch-during-playback

## Controls

Previous/Next, AutoNext, Details, Shuffle, PiP, Watch Party (placeholder), server grid.
