/// Common media / layout aspect ratios.
abstract final class ForjaAspects {
  /// Classic poster (2:3).
  static const double poster = 2 / 3;

  /// Landscape backdrop / continue-watching (16:9).
  static const double backdrop = 16 / 9;

  /// Square avatar / logo tile.
  static const double square = 1;

  /// Wide provider / logo card (~16:9).
  static const double logoCard = 16 / 9;

  /// Tall mood / category circle frame (1:1).
  static const double circle = 1;
}
