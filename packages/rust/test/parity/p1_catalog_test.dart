import 'package:flutter_test/flutter_test.dart';

// manga / books / catalog (bestsimilar) crates archived — see crates/archive/README.md
void main() {
  group('p1 catalog (archived crates)', () {
    test('placeholder', () {});
  }, skip: 'archived crate FFI removed from libffi');
}
