import 'package:flutter_test/flutter_test.dart';

// anilist / manga catalog crates archived — see crates/archive/README.md
void main() {
  group('anilist manga (archived crates)', () {
    test('placeholder', () {});
  }, skip: 'archived crate FFI removed from libffi');
}
