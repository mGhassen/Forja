namespace forja {
    string version();
    i64 add(i64 a, i64 b);
    boolean episode_matches(string filename, i32 season, i32 episode);
    string normalize_torrent_title(string title);
    string parse_scene_info_json(string title);
    string unpack_js(string source);
    string parse_hls_master_json(string master_url, string body);
    string decrypt_kisskh_body(string body, string? source_url);
    string build_movie_url(string provider_id, i64 tmdb_id);
    string build_tv_url(string provider_id, i64 tmdb_id, i32 season, i32 episode);
    string list_providers_json();
    string parse_m3u_json(string content);
    string decrypt_paste_response(string url_with_hash, string raw_response);
    string parse_stremio_manifest_json(string json);
    string build_stremio_resource_url(string addon_url, string resource_path);
    string parse_knaben_html_json(string html);
    string parse_tpb_html_json(string html);
    string parse_uindex_html_json(string html);
    string dedup_torrents_json(string results_json);
    boolean torrent_start(string magnet);
    void torrent_stop();
    boolean torrent_is_running();
};
