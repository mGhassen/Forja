---
description: Forja flat cinematic design system — tokens, colors, buttons, layout
globs: apps/forja/lib/**/*.dart
alwaysApply: false
---

# Forja design system (RFC-025 · RFC-095 · RFC-106)

**Source of truth (new components):** [`packages/forja_foundation`](../../packages/forja_foundation). Import the **file** you use — not `forja_foundation.dart` (gallery/test barrel). Never `hide Switch, Chip, …` against Material.

Architecture (who owns kit runner / product): [pack-product host](./forja-pack-product-host.mdc) · skill **forja-pack-product-host**. This rule is **paint only** — tokens, components, visual language.

Do **not** recreate `apps/forja/lib/shared/foundation/` or `shared/kit/`. Composers: `widgets/` / `components/` / `blocks/` / `brand/`. **Empty app shell** = compose foundation (`ShellBlock` + nav rail/body + brand). `apps/forja/lib/shell/` is chassis wire only (pack nav → body mount, platform, OTA) — **not** a second DS and **not** product chrome. Never add imports to `part of` files.

Composers take **props + callbacks only** (RFC-095). No `features/**` imports, god controllers, or product Riverpod in the DS package.

| New work | Package API |
|----------|-------------|
| Buttons | `Button`, `ButtonGroup` — `package:forja_foundation/components/button.dart` |
| Vertical flyout | `VerticalMenu.item` — `package:forja_foundation/components/vertical_menu.dart` |
| Logo filter menu | `LogoMenuRail` + `LogoMenuItem` — `package:forja_foundation/widgets/chrome/logo_menu_rail.dart` |
| Empty shell | `ShellBlock` — `package:forja_foundation/blocks/shell/shell_block.dart` |
| Brand | `ForjaLogo` / splash paint — `package:forja_foundation/brand/` |
| Protocol / kit **types** | `protocol/` · `kit/` — types only |

**Forbidden in app shell:** product tab ids (`home`, continue watching, search chrome), hub top bars, catalog section metrics APIs. Packs + kit painter own those.

## Visual language

- **Flat cinematic shell** — no `BackdropFilter`, frosted glass, or ambient glows on shell nav, home chrome, or player control bars.
- **Exception — sliding side panels:** media-details Sources uses `ForjaFrostedPanel` with `BackdropFilter`. Player overlays (Episodes, torrent files) pass a video screenshot as `frozenFrame` and blur that with `ImageFiltered` — do **not** put `BackdropFilter` over live video (freezes macOS). Do not scatter ad-hoc blur elsewhere.
- **Hero overlays** use `ForjaShellColors.cinematic` (fixed light-on-imagery), not `AppTheme.primaryColor`.
- **Spacing and breakpoints** come from `ShellTokens` / `DetailsTokens` / `EventCardTokens` — do not hardcode magic numbers that belong in tokens ([no-hardcode](./forja-foundation-no-hardcode.mdc)).

## Tokens

### `ShellTokens` — shell chrome + catalog density (generic; not a Home pack API)

| Area | Use |
|------|-----|
| Nav / top bar | `navRailWidth`, `homeTopBarHeight` (kit top-bar height token), `shellNavCompactMaxWidth` |
| Hero | `heroDesktopMinBodyWidth`, `heroTextColumnWidthDesktop`, `heroOverviewSlotHeightDesktop`, `homeSectionHorizontalPadding` |
| Catalog rows | `homeSectionTitleTop`, `bodyMaxWidthDesktop` |

### `DetailsTokens` — media details

| Area | Use |
|------|-----|
| Hero / body | `heroHeight`, `sectionSpacing`, `heroBodyOverlap`, `contentHorizontalPadding` |
| Sources panel | `sourcesPanelPadding` (player overlays keep `ShellTokens.playerSidePanelPadding`) |

### `EventCardTokens` — live event / match cards

| Area | Use |
|------|-----|
| Density | `desktopWidthScale`, `desktopHeightScale`, `desktopHeightMin` / `Max`, TV caption extras |
| Paint | `radius`, type sizes, play overlay sizes, `paintFallbackWidth` / `Height` |

Details gutters deliberately alias `ShellTokens.homeSectionHorizontalPadding` / `bodyMaxWidthDesktop` so details stay aligned with catalog rows.

When adding a reused constant: **shell/catalog → `ShellTokens`**, **details surfaces → `DetailsTokens`**, **event/match cards → `EventCardTokens`**. One-off padding inside a single widget is fine — grid density / clamps are not (see [no-hardcode](./forja-foundation-no-hardcode.mdc)).

## Colors (`ForjaShellColors`)

- Shell chrome: `textPrimary`, `textSecondary`, `iconMuted`, `borderSubtle`, `surfaceElevated`
- Selection / resume: `chipSelectedBg`, `chipSelectedBorder`, `progressFill`
- Menus / dropdowns: `ForjaShellColors.cinematic.menuSurface`
- Floating player menus: `PlayerPopupTokens` in `shared/player/controls/menus/player_popup_panel.dart` (translucent dark shell ~α0.82 like player side panels; select cards use idle `cardBg`, selected/hover/focus `accentFill` + green border — never solid `accent` fill)
- **Do not** use raw `Colors.white54` for new shell UI when a semantic token exists.

## Buttons

| Widget | When |
|--------|------|
| `Button(variant: ghost)` | Primary CTA on hero / details (Play, Watch Now) — text + optional icon, no fill |
| `Button(variant: plainIcon, size: icon)` | Secondary icon actions (+, info, overflow, close) — no border box |
| `Button(variant: outline, size: icon)` | Sparingly — bordered square; prefer `plainIcon` in hero chrome |
| `ForjaInteractive` | Custom hit targets that need hover/press scale (foundation / host glue) |

## Toggles

| Widget | When |
|--------|------|
| `ForjaSwitch` | All on/off toggles (settings rows, player chrome, etc.) |
| `forjaSwitchThemeData` | App `ThemeData.switchTheme` — also styles `SwitchListTile` that do not override colors |

Prefer `ForjaSwitch(scale: ForjaSwitch.settingsScale)` in dense settings lists. Do **not** style raw `Switch(` with ad-hoc `activeTrackColor` / `thumbColor`.

## Section titles

- Horizontal catalog rows: `ShellSectionTitle` or `ShellSectionTitle.titleStyle`
- Media details sections (Cast, Trailers): same `titleStyle` — plain text, no underline, no icon prefix

## Focus / TV

- Interactive list tiles and cards: wrap with `FocusableControl` from `app_theme.dart` (keyboard + focus ring).
- Platform layout and input: read `ShellScope.metrics` / `ShellScope.inputPolicy` — not `ShellTokens.isTvLayout` in widgets.
- Platform shell profile switches belong in foundation empty-shell primitives; app only picks profile and wires pack destinations into the body slot.

### Text inputs (desktop + TV) — focus only until OK / Enter

**Keyboard / D-pad land = focus/highlight only. Never activate.** Opening the keyboard, edit mode, or search chrome requires explicit OK / Select / Enter / tap — never `onFocusChange(true)` or autofocus-into-edit. Phone keeps type-on-focus.

| Widget | Role |
|--------|------|
| `TvBrowseTextField` | Search/query fields — browse until activate, then edit + IME |
| `SettingsTextField` | Settings rows — same browse/edit split |
| `ShellInputPolicy.browseTextUntilActivate` | Gate (desktop + leanback); do not reuse `leanbackOnly` |

Forbidden on desktop/TV: raw `TextField` that becomes editable / opens IME when focused; wiring focus listeners to `openSearch` / `beginEditing`. Full rule: `forja-tv-scope.mdc` → **text inputs**.

## Forbidden without explicit approval

- Magic scales, clamps, or fallback sizes for foundation cards/chrome — token first ([no-hardcode](./forja-foundation-no-hardcode.mdc))
- Ad-hoc `BackdropFilter` / `ImageFilter.blur` outside `ForjaFrostedPanel` (and existing IPTV frosted panels)
- Ad-hoc pill/chip styles that duplicate `TorrentSourceToggle` / chip selected tokens
- Ad-hoc `Switch(` / `SwitchListTile` color overrides — use `ForjaSwitch` / theme tokens
- Duplicating hero title or meta rendering — use `package:forja_foundation/widgets/details/` (see `forja-shared-ui.mdc`)
