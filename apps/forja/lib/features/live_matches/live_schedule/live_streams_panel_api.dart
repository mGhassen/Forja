/// Public Live Sports streams-panel surface (RFC-073 / RFC-084).
///
/// Implementation lives in the hub library
/// (`live_schedule/play/live_streams_panel.dart` as `LiveMatchStreamsPanel`).
/// Kit browse opens this panel via the host play path — Providers + Live TV,
/// native play only.
library;

export 'play/live_play_kit.dart';
export 'live_sports_browse_shell.dart';
