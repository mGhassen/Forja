# Foundation primitives

App-wide atomic UI — files are `forja_*` (tokens, shell scope, buttons, chips, kit leafs).

```dart
import 'package:forja/shared/foundation/primitives/primitives.dart';
```

Composers (`kit.categoryBar`, `kit.menu`, …) live under `../components/`.
