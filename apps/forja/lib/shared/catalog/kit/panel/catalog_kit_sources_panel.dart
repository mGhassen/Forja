/// Live Sports streams panel — kit documentation anchor.
///
/// Generic opaque list side-panel chrome is owned by:
/// - `CatalogKitListWidget` (`sidePanel`)
/// - `CatalogKitPanelHost` (via `CatalogHostListRegistry`)
///
/// The Live Sports feature registers `LiveSportsStreamsPanelHost`, which builds
/// `LiveSportsStreamsPage` from `package:forja/shared/live/panel/` directly.
/// Keep product streams UI out of kit — this file is a path/comment anchor only.
library;
