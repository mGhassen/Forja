import 'package:forja/shared/extractors/core/embed_extract_profile.dart';

/// Host sniff policy for `primewire`.
const primewireExtractProfile = EmbedExtractProfile(
  id: 'primewire',
  timeout: Duration(seconds: 60),
);
