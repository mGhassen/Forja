# Host sources (RFC-106 G11)

| Path | Role |
|------|------|
| `torrent/**` | Torrent panels / tiles / loading |
| `panel/kit_sources_live_tv_browse.dart` | Live TV channel browse chrome for sources panel |
| `panel/kit_resolve_panel_host.dart` | Live resolve `KitPanelHost` (Providers / Live TV tabs) |

Foundation stubs re-export until G14-E.
Generic `kit_sources_panel.dart` stays in foundation (opaque resolve UI).
